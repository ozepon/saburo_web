<%= form_for(line_liver) do |f| %>
  <% if line_liver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_liver.errors.count, "error") %> prohibited this line_liver from being saved:</h2>

      <ul>
      <% line_liver.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :line_liver_id %>
    <%= f.number_field :line_liver_id %>
  </div>

  <div class="field">
    <%= f.label :active %>
    <%= f.check_box :active %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
